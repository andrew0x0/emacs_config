[user]
	name = yuguo
	email = yuguo@deeproute.ai
[core]
	pager = 
	editor = emacs
[diff]
	tool = meld
[merge]
	tool = meld
[alias]
	diffall = git-diffall
	cmc ="!d="$(date -d "$1")" && shift && GIT_COMMITTER_DATE="$d" git commit --amend --date "$d""
[cola]
	spellcheck = false
